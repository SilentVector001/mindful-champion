import { LiveCoverageDashboard } from '@/components/media/live-coverage-dashboard';

export const metadata = {
  title: 'Live Coverage | Mindful Champion Media Center',
  description: 'Watch live pickleball tournaments, matches, and events in real-time',
};

export default function LiveCoveragePage() {
  return <LiveCoverageDashboard />;
}
