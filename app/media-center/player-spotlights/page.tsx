import { PlayerSpotlightCards } from '@/components/media/player-spotlight-cards';

export const metadata = {
  title: 'Player Spotlights | Mindful Champion',
  description: 'Featured pickleball players, their stats, achievements, and inspiring stories',
};

export default function PlayerSpotlightsPage() {
  return <PlayerSpotlightCards />;
}
