import { PhotoGallery } from '@/components/media/photo-gallery';

export const metadata = {
  title: 'Photo Gallery | Mindful Champion',
  description: 'Browse photos from tournaments, community events, training sessions, and facilities',
};

export default function GalleryPage() {
  return <PhotoGallery />;
}
